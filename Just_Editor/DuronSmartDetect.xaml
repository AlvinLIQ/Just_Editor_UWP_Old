<UserControl
    x:Class="Just_Editor.DuronSmartDetect"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Just_Editor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="250">

    <ScrollViewer Name="MainContent" Background="{x:Bind thisData.TopBar_BackgroundBrush, Mode=OneWay}" Padding="0, 0, 15, 0"
                  BorderBrush="{x:Bind thisData.ToolBar_BackgroundBrush, Mode=OneWay}" Style="{StaticResource EditorScrollViewer}"
                   >
        <StackPanel x:Name="ItemPanel" Background="{x:Bind thisData.ToolBar_BackgroundBrush, Mode=OneWay}" KeyDown="UserControl_KeyDown">
        </StackPanel>
    </ScrollViewer>
</UserControl>
